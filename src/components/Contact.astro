<section id="contact" class="contact-section">
    <div class="container">
        <!-- Título y descripción -->
        <div class="contact-intro">
            <h2>Contáctanos</h2>
            <p class="intro-text">
                Estamos aquí para ayudarte. Si tienes preguntas sobre nuestros servicios o necesitas asesoría 
                personalizada no dudes en contactarnos. Completa el formulario o usa 
                nuestros datos de contacto, y un miembro de nuestro equipo estará encantado de atenderte.
            </p>
        </div>

        <!-- Grid de 2 columnas -->
        <div class="contact-content">
            <!-- Columna izquierda: Info + Horario + Mapa -->
            <div class="left-side">
                <!-- Información de contacto -->
                <div class="info-section">
                    <h3>Información de contacto</h3>
                    
                    <div class="info-row">
                        <svg class="info-icon-small" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 2C8.13 2 5 5.13 5 9c0 5.25 7 13 7 13s7-7.75 7-13c0-3.87-3.13-7-7-7zm0 9.5c-1.38 0-2.5-1.12-2.5-2.5s1.12-2.5 2.5-2.5 2.5 1.12 2.5 2.5-1.12 2.5-2.5 2.5z"/>
                        </svg>
                        <p> Cervantes, Alvarado, Cartago, Costa Rica</p>
                    </div>

                    <div class="info-row">
                        <svg class="info-icon-small" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/>
                        </svg>
                        <p>+506 8313 1641</p>
                    </div>

                    <div class="info-row">
                        <svg class="info-icon-small" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/>
                        </svg>
                        <p>forzabox15@gmail.com</p>
                    </div>
                </div>

                <!-- Horario -->
                <div class="schedule-section">
                    <h3>Horario</h3>
                    <div class="schedule-rows">
                        <div class="schedule-row">
                            <span class="day">Lunes a Viernes:</span>
                            <span class="hours">6:00 a.m. - 7:30 p.m.</span>
                        </div>
                        <div class="schedule-row">
                            <span class="day">Sábado:</span>
                            <span class="hours">7:00 a.m. - 9:00 a.m.</span>
                        </div>
                        <div class="schedule-row">
                            <span class="day">Domingo:</span>
                            <span class="hours">Cerrado</span>
                        </div>
                    </div>
                </div>

                <!-- Mapa de Google -->
                <div class="map-wrapper">
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3930.5581623646985!2d-83.8096363!3d9.887385900000002!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x8fa0d970bddffb6d%3A0x14ec4c4ad2277697!2sForza%20Box!5e0!3m2!1ses-419!2scr!4v1765479386712!5m2!1ses-419!2scr" 
                    width="600" 
                    height="450" 
                    style="border:0;" 
                    allowfullscreen="" 
                    loading="lazy" 
                    referrerpolicy="no-referrer-when-downgrade">
                </iframe>
                </div>
            </div>

            <!-- Columna derecha: Formulario -->
            <div class="right-side">
                <div class="form-wrapper">
                    <h3>Ponte en contacto con nosotros</h3>
                    <form id="contact-form" action="https://formspree.io/f/xrbnoljj" method="POST">
                        <input type="text" name="nombre" placeholder="Nombre" required>
                        <input type="text" name="apellidos" placeholder="Apellidos" required>
                        <input type="email" name="email" placeholder="Correo electrónico" required>
                        <input type="text" name="asunto" placeholder="Asunto" required>
                        <textarea name="mensaje" rows="5" placeholder="Mensaje" required></textarea>
                        <input type="hidden" name="_subject" value="Nuevo mensaje de contacto - ForzaBox">
                        <button type="submit" class="btn-enviar">Enviar</button>
                    </form>
                    <div id="form-status" class="form-status hidden"></div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    /* ==================== SECCIÓN PRINCIPAL ==================== */
    .contact-section {
        padding: var(--spacing-lg) 0 var(--spacing-xl) 0;
        background: linear-gradient(
            to bottom,
            var(--accent-white) 0%,
            var(--gray-lighter) 50%,
            var(--accent-white) 100%
        );
        position: relative;
        overflow: hidden;
    }

    .contact-section::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 400px;
        background: radial-gradient(
            ellipse at top,
            rgba(0, 229, 204, 0.05) 0%,
            transparent 70%
        );
        pointer-events: none;
    }

    .contact-section .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 var(--spacing-md);
        position: relative;
        z-index: 1;
        width: 100%;
        box-sizing: border-box;
    }

    /* ==================== INTRO ==================== */
    .contact-intro {
        margin-bottom: 2.5rem;
        opacity: 0;
        transform: translateY(-20px);
        animation: fadeInDown 0.8s ease forwards;
    }

    .contact-intro h2 {
        font-size: clamp(1.75rem, 4vw, 2.5rem);
        color: var(--dark-accent);
        font-weight: 800;
        margin-bottom: 1rem;
        text-align: left;
    }

    .intro-text {
        font-size: var(--text-base);
        color: var(--gray-dark);
        line-height: 1.7;
        max-width: 100%;
        margin: 0;
        text-align: left;
    }

    /* ==================== GRID 2 COLUMNAS ==================== */
    .contact-content {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 3rem;
    }

    /* ==================== COLUMNA IZQUIERDA ==================== */
    .left-side {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        opacity: 0;
        transform: translateX(-30px);
        animation: slideInLeft 0.8s ease forwards 0.2s;
    }

    .left-side h3 {
        font-size: var(--text-xl);
        color: var(--dark-accent);
        font-weight: 700;
        margin-bottom: 1rem;
    }

    /* Información de contacto */
    .info-section {
        margin-bottom: 0;
    }

    .info-row {
        display: flex;
        align-items: flex-start;
        gap: 0.75rem;
        margin-bottom: 1rem;
        transition: transform 0.3s ease;
    }

    .info-row:hover {
        transform: translateX(5px);
    }

    .info-row:last-child {
        margin-bottom: 0;
    }

    .info-icon-small {
        width: 20px;
        height: 20px;
        min-width: 20px;
        color: var(--primary-cyan);
        margin-top: 2px;
        transition: all 0.3s ease;
    }

    .info-row:hover .info-icon-small {
        color: var(--cyan-light);
        transform: scale(1.1);
    }

    .info-row p {
        font-size: var(--text-base);
        color: var(--dark-accent);
        margin: 0;
        line-height: 1.6;
    }

    /* Horarios */
    .schedule-section {
        margin-bottom: 0;
    }

    .schedule-rows {
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .schedule-row {
        display: flex;
        justify-content: flex-start;
        align-items: baseline;
        font-size: var(--text-base);
        gap: 0.5rem;
        padding: 0.5rem 0;
        border-bottom: 1px solid var(--gray-lighter);
        transition: all 0.3s ease;
    }

    .schedule-row:last-child {
        border-bottom: none;
    }

    .schedule-row:hover {
        padding-left: 0.5rem;
        border-bottom-color: var(--primary-cyan);
    }

    .schedule-row .day {
        font-weight: 600;
        color: var(--dark-accent);
        min-width: 140px;
    }

    .schedule-row .hours {
        color: var(--gray-dark);
    }

    /* Mapa */
    .map-wrapper {
        width: 100%;
        height: 350px;
        border-radius: var(--radius-lg);
        overflow: hidden;
        box-shadow: var(--shadow-lg);
        transition: all 0.3s ease;
        border: 2px solid transparent;
    }

    .map-wrapper:hover {
        box-shadow: var(--shadow-xl);
        border-color: var(--cyan-transparent-20);
    }

    /* ==================== COLUMNA DERECHA - FORMULARIO ==================== */
    .right-side {
        background: var(--accent-white);
        border-radius: var(--radius-xl);
        padding: 2rem;
        box-shadow: var(--shadow-lg);
        border: 2px solid transparent;
        transition: all 0.4s ease;
        opacity: 0;
        transform: translateX(30px);
        animation: slideInRight 0.8s ease forwards 0.3s;
        width: 100%;
        box-sizing: border-box;
    }

    .right-side:hover {
        border-color: var(--primary-cyan);
        box-shadow: var(--shadow-xl);
    }

    .form-wrapper {
        width: 100%;
        box-sizing: border-box;
    }

    .form-wrapper h3 {
        font-size: var(--text-xl);
        color: var(--dark-accent);
        font-weight: 700;
        margin-bottom: 1.5rem;
    }

    .right-side form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .right-side input,
    .right-side textarea {
        width: 100%;
        max-width: 100%;
        padding: 0.875rem 1.25rem;
        font-size: var(--text-base);
        color: var(--dark-accent);
        background: var(--gray-lighter);
        border: 2px solid transparent;
        border-radius: var(--radius-md);
        font-family: var(--font-primary);
        transition: all 0.3s ease;
        box-sizing: border-box;
    }

    .right-side input:focus,
    .right-side textarea:focus {
        outline: none;
        border-color: var(--primary-cyan);
        background: var(--accent-white);
        box-shadow: 0 0 0 4px var(--cyan-transparent);
    }

    .right-side input::placeholder,
    .right-side textarea::placeholder {
        color: var(--gray-dark);
    }

    .right-side textarea {
        resize: vertical;
        min-height: 120px;
    }

    .btn-enviar {
        width: 100%;
        padding: 1rem 2rem;
        background: linear-gradient(135deg, var(--primary-cyan) 0%, var(--cyan-dark) 100%);
        color: var(--dark-accent);
        border: none;
        border-radius: var(--radius-md);
        font-size: var(--text-base);
        font-weight: 700;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        margin-top: 0.5rem;
        box-shadow: 0 4px 20px rgba(0, 229, 204, 0.4);
        position: relative;
        overflow: hidden;
    }

    .btn-enviar::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.3);
        transform: translate(-50%, -50%);
        transition: width 0.6s ease, height 0.6s ease;
    }

    .btn-enviar:hover::before {
        width: 300px;
        height: 300px;
    }

    .btn-enviar:hover {
        background: var(--primary-cyan);
        transform: translateY(-3px);
        box-shadow: 0 8px 32px rgba(0, 229, 204, 0.6);
    }

    .btn-enviar:active {
        transform: translateY(-1px);
    }

    /* ==================== ANIMACIONES ==================== */
    @keyframes fadeInDown {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes slideInLeft {
        from {
            opacity: 0;
            transform: translateX(-30px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    @keyframes slideInRight {
        from {
            opacity: 0;
            transform: translateX(30px);
        }
        to {
            opacity: 1;
            transform: translateX(0);
        }
    }

    /* ==================== RESPONSIVE ==================== */
    @media (max-width: 1024px) {
        .contact-content {
            gap: 2rem;
        }

        .map-wrapper {
            height: 300px;
        }
    }

    @media (max-width: 768px) {
        .contact-section {
            padding: var(--spacing-md) 0 var(--spacing-lg) 0;
        }

        .contact-section .container {
            padding: 0 0.75rem;
            max-width: 100%;
        }

        .contact-intro {
            margin-bottom: 2rem;
        }

        .contact-content {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .left-side,
        .right-side {
            animation: fadeInUp 0.8s ease forwards;
        }

        .right-side {
            padding: 1.25rem;
        }

        .map-wrapper {
            height: 280px;
        }

        .schedule-row .day {
            min-width: 120px;
        }
    }

    @media (max-width: 480px) {
        .contact-section {
            padding: var(--spacing-sm) 0 var(--spacing-md) 0;
        }

        .contact-section .container {
            padding: 0 0.375rem;
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
        }

        .contact-content {
            gap: 1rem;
        }

        .contact-intro {
            margin-bottom: 1.5rem;
        }

        .contact-intro h2 {
            font-size: 1.5rem;
        }

        .intro-text {
            font-size: var(--text-sm);
        }

        .left-side h3,
        .form-wrapper h3 {
            font-size: var(--text-lg);
        }

        .right-side {
            padding: 0.875rem;
            border-radius: var(--radius-lg);
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
            margin: 0;
            border: none;
        }

        .form-wrapper {
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
        }

        .right-side form {
            gap: 0.625rem;
            width: 100%;
            max-width: 100%;
        }

        .right-side input,
        .right-side textarea {
            padding: 0.5rem;
            font-size: var(--text-sm);
            width: 100%;
            max-width: 100%;
            margin: 0;
            border-width: 1px;
        }

        .btn-enviar {
            padding: 0.625rem;
            font-size: var(--text-sm);
            width: 100%;
            max-width: 100%;
            box-sizing: border-box;
            margin-top: 0.125rem;
        }

        .info-row p,
        .schedule-row {
            font-size: var(--text-sm);
        }

        .schedule-row {
            flex-direction: column;
            gap: 0.25rem;
        }

        .schedule-row .day {
            min-width: auto;
        }

        .map-wrapper {
            height: 250px;
        }

        .form-status {
            padding: 0.75rem;
            font-size: var(--text-sm);
            margin-top: 0.75rem;
            border-width: 1px;
        }
    }

    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    /* ==================== MENSAJES DE ESTADO DEL FORMULARIO ==================== */
    .form-status {
        margin-top: 1.5rem;
        padding: 1rem 1.5rem;
        border-radius: var(--radius-md);
        font-size: var(--text-base);
        font-weight: 600;
        text-align: center;
        animation: fadeInUp 0.4s ease forwards;
    }

    .form-status.hidden {
        display: none;
    }

    .form-status.success {
        background: linear-gradient(135deg, rgba(0, 229, 204, 0.1), rgba(0, 229, 204, 0.05));
        border: 2px solid var(--primary-cyan);
        color: var(--dark-accent);
    }

    .form-status.error {
        background: rgba(255, 76, 76, 0.1);
        border: 2px solid var(--error);
        color: var(--error);
    }

    .form-status.loading {
        background: var(--gray-lighter);
        border: 2px solid var(--gray-light);
        color: var(--gray-dark);
    }
</style>

<script>
    const form = document.getElementById('contact-form') as HTMLFormElement;
    const formStatus = document.getElementById('form-status') as HTMLElement;

    if (form) {
        form.addEventListener('submit', async (e) => {
            e.preventDefault();
            
            // Mostrar mensaje de carga
            formStatus.className = 'form-status loading';
            formStatus.textContent = 'Enviando mensaje...';
            
            const formData = new FormData(form);
            
            try {
                const response = await fetch(form.action, {
                    method: 'POST',
                    body: formData,
                    headers: {
                        'Accept': 'application/json'
                    }
                });
                
                if (response.ok) {
                    // Éxito
                    formStatus.className = 'form-status success';
                    formStatus.textContent = '¡Gracias! Tu mensaje ha sido enviado con éxito. Te responderemos pronto.';
                    form.reset();
                } else {
                    // Error del servidor
                    formStatus.className = 'form-status error';
                    formStatus.textContent = 'Hubo un problema al enviar tu mensaje. Por favor, intenta nuevamente.';
                }
            } catch (error) {
                // Error de red
                formStatus.className = 'form-status error';
                formStatus.textContent = 'Error de conexión. Verifica tu internet e intenta nuevamente.';
            }
        });
    }
</script>